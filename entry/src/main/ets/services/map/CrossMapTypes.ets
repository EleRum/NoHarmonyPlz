/**
 * CrossMapTypes removed — replaced by MapTypes.ets + IMapService + MapJsBridge
 * All types (LatLng, LatLngBounds, MarkerOptions, PolylineOptions) are now in MapTypes.ets
 * Map controller operations use IMapService interface implemented by MapJsBridge
 */
